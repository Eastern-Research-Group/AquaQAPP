{
  "name": "aquaqapp",
  "scripts": {
    "heroku-prebuild": "cd client && rm -rf node_modules && npm install && npm run build && cd ../server && npm install",
    "heroku-postinstall": "",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": "10.15.3"
  }
}
