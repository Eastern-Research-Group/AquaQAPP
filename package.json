{
  "name": "aquaqapp",
  "scripts": {
    "heroku-prebuild": "cd client && npm install && cd ../server && npm install",
    "heroku-postinstall": "cd client && npm install @vue/cli-service && npm run build",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": "10.15.3"
  }
}
