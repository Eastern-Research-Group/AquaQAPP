{
  "name": "aquaqapp",
  "scripts": {
    "heroku-prebuild": "cd client && npm install && ./node_modules/.bin/vue-cli-service build && cd ../server && npm install",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": "10.15.3"
  }
}
