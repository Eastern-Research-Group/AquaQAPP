{
  "name": "aquaqapp",
  "scripts": {
    "heroku-prebuild": "cd client && npm install && npx vue-cli-service build && cd ../server && npm install",
    "heroku-postinstall": "",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": "10.15.3"
  }
}
